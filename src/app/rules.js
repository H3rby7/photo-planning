/**
 * Check if two shots take place in the same location
 * 
 * @param {Shot} a Shot A
 * @param {Shot} b Shot B
 * 
 * @returns {boolean} true if it is same location, else false
 */

function isSameLocation(a, b) {
  return true;
}